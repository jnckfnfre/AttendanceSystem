@model AttendanceSystem.API.Models.Quiz
@{
  var qs          = Model.Questions.ToList();
  var utdId       = ViewData["UtdId"]       as string ?? "";
  var courseId    = ViewData["CourseId"]    as string ?? "";
  var sessionDate = ((DateTime)ViewData["SessionDate"]).ToString("o");
  var quizId      = Model.QuizId;
}

<form action="/api/Submissions" method="post">
  @for (int i = 0; i < qs.Count; i++)
  {
      var q = qs[i];
      <div class="mb-4">
        <p><strong>Question @(i+1):</strong> @q.Text</p>
        <label><input type="radio" name="Answers[@i]"       value="A"required /> A. @q.OptionA </label><br />
        <label><input type="radio" name="Answers[@i]" value="B" /> B. @q.OptionB</label><br />
        <label><input type="radio" name="Answers[@i]" value="C" /> C. @q.OptionC</label><br />
        <label><input type="radio" name="Answers[@i]" value="D" /> D. @q.OptionD</label>
      </div>
  }

  <!-- Hidden metadata -->
  <input type="hidden" name="UtdId"       value="20213456"       />
  <input type="hidden" name="CourseId"    value="@courseId"    />
  <input type="hidden" name="SessionDate" value="@sessionDate" />
  <input type="hidden" name="QuizId"      value="@quizId"      />
  <input type="hidden" name="Status"      value="Present"      />

  <button type="submit" class="btn btn-success">Submit Answers</button>
</form>
